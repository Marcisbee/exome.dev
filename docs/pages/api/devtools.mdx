# devtools

```ts twoslash
// @noErrors
import {
  exomeReduxDevtools,
  unstableExomeDevtools
} from "exome/devtools";
```

<div className="api-explore">

<p className="api-explore-comment">
Subscribes to [Redux DevTools](https://chromewebstore.google.com/detail/redux-devtools/lmhkpmbekcpmknklioeibfkpmmfibljd).
</p>
```ts twoslash
// @noErrors
import { Middleware } from "exome";
import { exomeReduxDevtools as source } from "exome/devtools";
type ReduxDevtoolsConfig = Parameters<typeof source>[0]
// ---cut---
function exomeReduxDevtools({
  name,
  maxAge,
  actionsBlacklist,
}: ReduxDevtoolsConfig): Middleware;
```

</div>

```ts [Example] twoslash
// @noErrors
import { addMiddleware } from "exome"
import { exomeReduxDevtools } from "exome/devtools"
// ---cut---
addMiddleware(
  exomeReduxDevtools({
    name: "App name"
  })
)
```

---

<div className="api-explore">
<p className="api-explore-comment">
Subscribes to [Exome Developer Tools](https://chromewebstore.google.com/detail/exome-developer-tools/pcanmpamoedhpfpbjajlkpicbikbnhdg).
</p>
```ts twoslash
// @noErrors
import { Middleware } from "exome";
import { unstableExomeDevtools as source } from "exome/devtools";
type ExomeDevtoolsConfig = Parameters<typeof source>[0]
// ---cut---
function unstableExomeDevtools({
  name,
  maxAge,
  ignoreListActions,
  ignoreListStores,
}: ExomeDevtoolsConfig): Middleware;
```
</div>

```ts [Example] twoslash
// @noErrors
import { addMiddleware } from "exome"
import { unstableExomeDevtools } from "exome/devtools"
// ---cut---
addMiddleware(
  unstableExomeDevtools({
    name: "App name"
  })
)
```

:::tip[NOTE]
Exome devtools is currently in alpha and may not be stable.
:::
